<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Cards</title>
    <style>
        body {
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }

        .card {
            background: #fff;
            padding: 15px;
            border-radius: 12px;
            margin: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 1100px;
            border-left: 5px solid #007bff;
            position: relative;
        }

        .route-info {
            background: #e3f2fd;
            padding: 10px;
            border-radius: 8px;
            font-size: 14px;
            font-weight: bold;
            margin-bottom: 8px;
        }

        .route-details {
            background: #f1f3f4;
            padding: 8px;
            border-radius: 8px;
            margin-bottom: 12px;
        }

        .route-details p {
            margin: 4px 0;
            font-weight: bold;
        }

        .trip {
            padding: 8px;
            background: #fff3cd;
            border-radius: 8px;
            margin-bottom: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            max-height: 200px;
            overflow: auto;
            font-size: 14px;
        }

        .buttons {
            display: flex;
            justify-content: flex-start;
            margin-top: 8px;
        }

        .edit-btn-container {
            display: flex;
            justify-content: flex-end;
            margin-top: 8px;
        }

        .back-btn {
            background-color: #007bff;
            color: white;
            padding: 8px 12px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.3s;
            font-size: 14px;
        }

        .back-btn:hover {
            background-color: #0056b3;
        }

        .trip-buttons {
            display: flex;
            justify-content: flex-end;
            margin-top: 8px;
        }

        .trip-edit-btn,
        .edit-btn {
            background-color: #28a745;
            color: white;
            padding: 6px 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.3s;
            font-size: 12px;
        }

        .trip-edit-btn:hover,
        .edit-btn:hover {
            background-color: #218838;
        }
    </style>
</head>

<body>

    <div class="card">
        <!-- Yo'nalish nomi, Qayerdan, Qayerga -->
        <div class="route-info">
            <p><strong>Yo'nalish nomi:</strong> {{trip.routes.name}}</p>
            <p><strong>Qayerdan:</strong> {{trip.routes.from}}</p>
            <p><strong>Qayerga:</strong> {{trip.routes.to}}</p>
            <div class="edit-btn-container">
                <button class="edit-btn">Tahrirlash</button>
            </div>
        </div>

        <div class="trip">
            <p><strong>Ketish kuni:</strong> {{trip.departure_date}}</p>
            <p><strong>Ketish vaqti:</strong> {{trip.departure_time}}</p>
            <p><strong>Kelish kuni:</strong> {{trip.arrival_date}}</p>
            <p><strong>Kelish vaqti:</strong> {{trip.arrival_time}}</p>
            <p><strong>Chipta narxi:</strong> {{trip.ticket_price}}</p>
            <div class="trip-buttons">
                <button class="trip-edit-btn">Tahrirlash</button>
            </div>
        </div>

        <!-- Orqaga tugmasi joyida qoldi -->
        <div class="buttons">
            <button class="back-btn">Orqaga</button>
        </div>
    </div>

</body>

</html>