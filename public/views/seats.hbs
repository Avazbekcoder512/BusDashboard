<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Avtobus O'rindiqlari</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #5A67D8;
            --danger: #E53E3E;
            --danger-light: rgba(229, 62, 62, 0.2);
            --bg-gradient: linear-gradient(135deg, #667eea, #764ba2);
            --card-bg: rgba(255, 255, 255, 0.15);
            --blur: 10px;
            --text-light: #FFF;
            --text-dark: #2D3748;
            --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.1);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--bg-gradient);
            font-family: 'Poppins', sans-serif;
            color: var(--text-dark);
            padding: 20px;
        }

        .container {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
            gap: 20px;
            width: 100%;
            max-width: 1000px;
        }

        .grid-item {
            position: relative;
            background: var(--card-bg);
            border-radius: 16px;
            backdrop-filter: blur(var(--blur));
            box-shadow: var(--shadow-lg);
            padding: 16px;
            text-align: center;
            transition: transform 0.3s ease;
        }

        .grid-item:hover {
            transform: translateY(-5px);
        }

        .grid-item .seat-number {
            font-size: 1.4rem;
            font-weight: 600;
            margin-bottom: 8px;
        }

        .grid-item .badge {
            display: inline-block;
            padding: 4px 10px;
            border-radius: 12px;
            font-size: 0.75rem;
            font-weight: 500;
            margin-bottom: 12px;
            color: var(--text-light);
            background: var(--primary);
        }

        .grid-item[data-status="band"] .badge {
            background: var(--danger);
        }

        .grid-item .select {
            margin-top: 8px;
        }

        input[type="checkbox"] {
            width: 1.2em;
            height: 1.2em;
            accent-color: var(--primary);
            cursor: pointer;
            transform: scale(1.2);
        }

        /* Rezerv qilingan o'rin */
        .grid-item[data-status="band"] {
            background: var(--danger-light);
            color: var(--text-light);
        }

        .grid-item[data-status="band"] input[type="checkbox"] {
            pointer-events: none;
            opacity: 0.6;
            cursor: not-allowed;
        }
    </style>
</head>

<body>
    <div class="container">
        {{#each trip.seats}}
        <div class="grid-item" data-status="{{status}}">
            <div class="seat-number">{{seatNumber}}</div>
            <div class="badge">{{status}}</div>
            <div class="select">
                <input type="checkbox">
            </div>
        </div>
        {{/each}}
    </div>
</body>

</html>